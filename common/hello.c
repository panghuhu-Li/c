// #include "arrList.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#define ABC(x) (x) * (x)

#define HIGH 30
#define WIDE 80

void swap(int a, int b) {
    printf("a:%d b:%d", a, b);
}

int main() {
    int i;
    printf("%d",i);
    long number=200/1000;
    printf("%ld",number);
    
}
